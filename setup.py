from setuptools import setup

setup(name='sinf',
      use_scm_version=True,
      setup_requires=['setuptools_scm'],
      description='slices iterative normalizing flow',
      url='https://github.com/biweidai/SINF',
      author='Biwei Dai',
      packages=['sinf']
      )
